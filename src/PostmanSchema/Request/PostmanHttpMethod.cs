namespace Swashbuckle.SwaggerToPostman.PostmanSchema.Request
{
    public enum PostmanHttpMethod
    {
        GET, PUT, POST, PATCH, DELETE, COPY, HEAD, OPTIONS, LINK, UNLINK, PURGE, LOCK, UNLOCK, PROPFIND, VIEW
    }
}